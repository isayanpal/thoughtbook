import { Auth } from "../components/Auth";

const Signup = () => {
  return (
    <div className="flex justify-center items-center">
      <Auth type="signup" />
    </div>
  );
};

export default Signup;
