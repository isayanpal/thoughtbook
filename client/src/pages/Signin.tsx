import { Auth } from "../components/Auth";

const Signin = () => {
  return (
    <div className="flex justify-center items-center">
      <Auth type="signin" />
    </div>
  );
};

export default Signin;
